[pytest]
# The Lambda modules live in src/app and import each other by bare name
# (e.g. `import config`), matching how they resolve inside the Lambda package.
# Add that dir to the import path so tests can import them the same way.
pythonpath = src/app
testpaths = tests
